name: My First GitHub Action

on:
  push:
    branches:
      - main

jobs:
  demo-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hello Bhargav! GitHub Actions is working 🚀"
